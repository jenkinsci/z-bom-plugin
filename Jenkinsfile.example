pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }

        stage('Z-BOM Scan') {
            steps {
                zbomScan(
                    serverUrl: 'https://zbom.example.com',
                    credentialsId: 'Z_BOM_TOKEN',
                    type: 'code',
                    failOn: 'none',
                    timeoutSeconds: 1800,
                    intervalSeconds: 10,
                    webUrl: 'https://zbom.example.com'
                )
            }
        }
    }
}
